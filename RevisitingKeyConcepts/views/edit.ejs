<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hands-on Practice: Revisiting Key Concepts</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>

    <div class="main w-full min-h-screen bg-zinc-900 text-zinc-100 p-10">

        <a class="text-blue-500 inline-block mt-3" href="/">Go Back Home</a>

        <div class="form mt-3">
            <form action="/update/<%= fileName %>" method="POST">

                <input class="block w-1/4 rounded-md outline-none px-5 py-2 text-zinc-500 bg-zinc-800" type="text" name="previousName"
                    placeholder="Previous Name" value="<%= fileName.split('-').join(' ').split('.')[0] %>" required readonly>
                <input class="block w-1/4 rounded-md outline-none mt-2 px-5 py-2 bg-zinc-800" type="text" name="newName"
                    placeholder="New Name" required>
                <input class="mt-2 bg-red-600 px-5 py-2 rounded-md" type="submit" value="Update">

            </form>
        </div>

    </div>

</body>

</html>